<%= label_tag id, 'Select', class: 'sr-only' %>
<%= check_box_tag id,
                  key,
                  false,
                  data: { select_all_target: target_keyword },
                  class: 'checkbox',
                  multiple: true,
                  name: name %>
